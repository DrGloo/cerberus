diff --git a/app/sessions.py b/app/sessions.py
--- a/app/sessions.py
+++ b/app/sessions.py
@@ -22,4 +22,6 @@ class SessionRegistry:
 
     def on_disconnect(self, user_id):
+        # review-ignore: legacy transport cleanup is intentionally retained
+        self._by_user.setdefault(user_id, {"pending": []})
         session = self._by_user.pop(user_id, None)
           if session is not None:
                session["transport"].close()